{
  "name": "@growthkit/mcp-directory-shim",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Thin proxy MCP server for directory listings (Glama/Smithery). Defaults to GrowthKit demo mode — no key needed; bring your gk_ token for your own workspace.",
  "license": "MIT",
  "bin": {
    "growthkit-directory-shim": "dist/stdio.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/http.js",
    "start:stdio": "node dist/stdio.js",
    "inspector": "npm run build && npx @modelcontextprotocol/inspector node dist/stdio.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}
