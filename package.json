{
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.22.0",
    "vitest": "4.1.11",
    "wrangler": "4.124.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "deploy": "echo 'Verboten. Deploy = Push auf main.' && exit 1"
  }
}
